# Crux EPUB Reader — Agent Progress

Project overview: Crux is a SwiftUI + SwiftData EPUB reader with AI-powered margin notes, annotations, and multi-provider AI support. This log tracks session-level progress and verification notes.

## Session log
- 2026-01-26T07:55:00Z | branch: feature/next-improvements | Feature: ai_prompt_explain_analyze
  - Summary: Updated AI annotation prompts to require explanation + analysis, refined ThreadPanel UI with clearer analysis CTA, retry control, and anchored input bar.
  - Files touched: `Shared/Services/ClaudeProvider.swift`, `Shared/Services/OpenAIProvider.swift`, `Shared/Services/CustomProvider.swift`, `Shared/Services/ClaudeService.swift`, `Shared/Views/ThreadPanel.swift`
  - Tests run: Not run (needs manual UI verification per PRE_FLIGHT_CHECKLIST.md)
  - Follow-ups: Launch app, validate AI output structure and ThreadPanel UX, confirm iOS/macOS layout
- 2026-01-26T08:59:03Z | branch: feature/next-improvements | Feature: ai_010_thread_actions
  - Summary: Added ThreadPanel action bar for AI responses with copy and save/append-to-note actions plus toast feedback.
  - Files touched: `Shared/Views/ThreadPanel.swift`
  - Tests run: Not run (manual UI verification needed)
  - Follow-ups: Verify copy/save actions in ThreadPanel and confirm Notes view reflects saved annotations
- 2026-06-04T00:00:00Z | branch: feature/next-improvements | Feature: providers_deepseek_minimax_kimi + toc_html_strip
  - Summary: (1) Added DeepSeek, MiniMax, and Kimi (Moonshot) as first-class AI providers — new `ProviderType` cases with default endpoints, model lists, subtitles, and SF Symbols; factory routes all three through the OpenAI-compatible `OpenAIProvider` (native SSE streaming, Bearer auth). (2) Made the cloud-provider model field editable with a "Suggested models" menu so custom / newly-released model names work and so "custom endpoints" are fully supported. (3) Fixed HTML tags appearing in the Table of Contents: new `cleanTitle` helper in `EPUBParser` strips nested markup + decodes entities + collapses whitespace; applied in `parseNavList` (EPUB3 nav — anchor regex widened from `[^<]+` to `[\s\S]*?` and href now accepts single/double quotes), `processNavPointRecursive` (EPUB2 NCX `<text>`), and `extractChapterTitle` (spine/HTML fallback headings). (4) Verified LM Studio provider end-to-end (routing, discovery, streaming) — already complete; no change needed.
  - Files touched: `Shared/Models/AIProviderConfig.swift`, `Shared/Services/AIProvider.swift`, `Shared/Views/SettingsView.swift`, `Shared/Services/EPUBParser.swift`
  - Tests run: `xcodebuild -scheme Crux_macOS` ⇒ BUILD SUCCEEDED; `xcodebuild -scheme Crux_iOS -destination 'iPhone 17 Pro'` ⇒ BUILD SUCCEEDED. Standalone Swift verification of cleanTitle + anchor regex ⇒ 5/5 cases pass (incl. nested `<span>`/`<i>`/`<br>` markup the old regex failed on).
  - Follow-ups: Manual smoke test of each new provider with a real API key; confirm TOC renders clean titles for an EPUB whose nav uses nested spans.
- 2026-06-04T01:00:00Z | branch: feature/next-improvements | Feature: ios_target_build_restored
  - Summary: The iOS target had never been built in recent sessions and was fully broken (every recent build in the log is macOS-only). Restored it to BUILD SUCCEEDED without disturbing macOS. Root causes fixed: (1) `NSImage` used directly in shared views `RecentBooksSection`/`BookDetailView` → switched to the cross-platform cache-backed `CachedCoverView`. (2) `Color(.controlBackgroundColor)` / `.windowBackgroundColor` / `.textBackgroundColor` (macOS-only `NSColor` semantics) used in 9 shared views → added `Color.crux*Background` cross-platform shims in `LiquidGlass.swift` and replaced all 25 usages. (3) `Color`'s `toHex()` used `NSColor` unconditionally → platform-split with `UIColor`. (4) Whole-file `#if os(macOS)` gates wrongly excluded the cross-platform `SettingsView` and `AIPromptSettingsView` (their subviews `GeneralSettingsView`/`ReaderSettingsView` are needed by iOS's `iOSSettingsView`, and the `Color(hex:)` extension lives in SettingsView) → removed the spurious gates; the genuinely macOS-only bits inside (NSWorkspace, NSColor) are individually guarded. (5) `.pickerStyle(.radioGroup)` (macOS-only) in BookmarksView/BookDetailView/NotesView → `#if os(macOS)` radioGroup / `#else` segmented. (6) `AppleIntelligenceProvider` FoundationModels APIs are iOS 26.0, but were annotated `iOS 18.4` → corrected all `@available`/`#available` to `iOS 26.0`. (7) macOS-only `performRestore()` was called from an unguarded restore `.sheet` in LibraryView → gated the sheet to `#if os(macOS)`.
  - Files touched: `Shared/Views/RecentBooksSection.swift`, `Shared/Views/BookDetailView.swift`, `Shared/Views/LiquidGlass.swift`, `Shared/Views/SettingsView.swift`, `Shared/Views/AIPromptSettingsView.swift`, `Shared/Views/BookmarksView.swift`, `Shared/Views/NotesView.swift`, `Shared/Views/TableOfContentsView.swift`, `Shared/Views/StatisticsView.swift`, `Shared/Views/StreaksView.swift`, `Shared/Views/ReadingGoalsView.swift`, `Shared/Views/LibraryView.swift`, `Shared/Services/AppleIntelligenceProvider.swift`, `Shared/Services/AIProvider.swift`, `Shared/Models/AIProviderConfig.swift`
  - Tests run: `xcodebuild -scheme Crux_iOS -destination 'iPhone 17 Pro' build` ⇒ BUILD SUCCEEDED; `xcodebuild -scheme Crux_macOS build` ⇒ BUILD SUCCEEDED (no regression).
  - Follow-ups: iOS now compiles but its runtime layout/behavior is unverified (the app was de-facto macOS-only). A dedicated iOS QA pass should exercise Library, Reader, Settings (iOSSettingsView), and AI flows on device/simulator before shipping iOS.
